# CRUD_operations-using-springBoot
Spring Boot REST API performing CRUD operations on a Book entity using Java, Spring Data JPA (CrudRepository), and MySQL. Implements layered architecture with Controller, Service, and Repository for clean separation of concerns and database interaction.
